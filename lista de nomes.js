// Lista com nomes
let nomes = ["Gabi", "Grazi", "Mariana", "Pedro", "Yasmin"];

// Percorre os índices do array
for (let indice in nomes) {

    // Mostra a posição e o nome correspondente
    console.log(`Posição ${indice}: ${nomes[indice]}`);

}